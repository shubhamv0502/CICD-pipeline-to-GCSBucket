name: My Workflow
on: [push]
    paths:
     - 'index.html'
     - 'css.txt'
jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        with:
          ref: main # checkout the main branch
